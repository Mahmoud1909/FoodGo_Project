# 🔧 حل مشكلة قاعدة البيانات - Access Denied

## ⚠️ المشكلة

```
SQLSTATE[HY000] [1044] Access denied for user 'laravel'@'localhost' to database 'foodgo'
```

هذا يعني أن:
- بيانات الاتصال بقاعدة البيانات في ملف `.env` **خاطئة**
- أو المستخدم `laravel` **لا يملك صلاحيات** على قاعدة البيانات `foodgo`
- أو قاعدة البيانات `foodgo` **غير موجودة**

---

## ✅ الحل خطوة بخطوة

### **الخطوة 1: تحقق من ملف `.env`**

افتح ملف `.env` في مجلد المشروع وتحقق من إعدادات قاعدة البيانات:

```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=foodgo          # تأكد من اسم قاعدة البيانات
DB_USERNAME=laravel         # تأكد من اسم المستخدم
DB_PASSWORD=                # تأكد من كلمة المرور
```

**⚠️ مهم:** استبدل القيم بأرقام قاعدة البيانات الصحيحة!

---

### **الخطوة 2: تحقق من قاعدة البيانات في MySQL**

#### أ) افتح MySQL Command Line:

```bash
mysql -u root -p
```

#### ب) تحقق من وجود قاعدة البيانات:

```sql
SHOW DATABASES;
```

إذا لم تكن قاعدة البيانات `foodgo` موجودة، أنشئها:

```sql
CREATE DATABASE foodgo CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

#### ج) تحقق من وجود المستخدم:

```sql
SELECT User, Host FROM mysql.user WHERE User='laravel';
```

إذا لم يكن المستخدم موجوداً، أنشئه:

```sql
CREATE USER 'laravel'@'localhost' IDENTIFIED BY 'your_password_here';
```

**⚠️ استبدل `your_password_here` بكلمة المرور التي تريدها!**

#### د) أعط المستخدم صلاحيات على قاعدة البيانات:

```sql
GRANT ALL PRIVILEGES ON foodgo.* TO 'laravel'@'localhost';
FLUSH PRIVILEGES;
```

#### هـ) تحقق من الصلاحيات:

```sql
SHOW GRANTS FOR 'laravel'@'localhost';
```

---

### **الخطوة 3: حدّث ملف `.env`**

بعد إنشاء المستخدم وقاعدة البيانات، حدّث ملف `.env`:

```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=foodgo
DB_USERNAME=laravel
DB_PASSWORD=your_password_here    # كلمة المرور التي أنشأتها
```

---

### **الخطوة 4: اختبر الاتصال**

#### أ) من سطر الأوامر:

```bash
php artisan tinker
```

ثم في Tinker:

```php
DB::connection()->getPdo();
```

إذا ظهرت رسالة نجاح، الاتصال يعمل! اكتب `exit` للخروج.

#### ب) أو استخدم أمر Laravel:

```bash
php artisan db:show
```

---

### **الخطوة 5: شغّل Migrations**

إذا كانت قاعدة البيانات جديدة، شغّل migrations:

```bash
php artisan migrate
```

---

## 🔄 حلول بديلة

### **الحل 1: استخدام مستخدم root (للتطوير فقط!)**

⚠️ **تحذير:** هذا الحل **للتطوير المحلي فقط**، لا تستخدمه في الإنتاج!

```env
DB_USERNAME=root
DB_PASSWORD=your_root_password
```

### **الحل 2: إنشاء قاعدة بيانات جديدة**

إذا كنت تريد إنشاء قاعدة بيانات جديدة:

```sql
CREATE DATABASE foodgo_new CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'foodgo_user'@'localhost' IDENTIFIED BY 'secure_password';
GRANT ALL PRIVILEGES ON foodgo_new.* TO 'foodgo_user'@'localhost';
FLUSH PRIVILEGES;
```

ثم حدّث `.env`:

```env
DB_DATABASE=foodgo_new
DB_USERNAME=foodgo_user
DB_PASSWORD=secure_password
```

---

## 🛠️ حلول للمشاكل الشائعة

### **المشكلة 1: "Unknown database 'foodgo'"**

**الحل:** قاعدة البيانات غير موجودة. أنشئها:

```sql
CREATE DATABASE foodgo CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### **المشكلة 2: "Access denied for user"**

**الحل:** المستخدم لا يملك صلاحيات. أعطه الصلاحيات:

```sql
GRANT ALL PRIVILEGES ON foodgo.* TO 'laravel'@'localhost';
FLUSH PRIVILEGES;
```

### **المشكلة 3: "Can't connect to MySQL server"**

**الحل:** تحقق من:
- MySQL يعمل: `sudo service mysql status` أو `sudo systemctl status mysql`
- `DB_HOST` صحيح في `.env`
- الجدار الناري يسمح بالاتصال

### **المشكلة 4: "Table doesn't exist"**

**الحل:** شغّل migrations:

```bash
php artisan migrate
```

---

## 📋 قائمة فحص سريعة

- [ ] قاعدة البيانات `foodgo` موجودة في MySQL
- [ ] المستخدم `laravel` موجود في MySQL
- [ ] المستخدم لديه صلاحيات على قاعدة البيانات
- [ ] ملف `.env` يحتوي على بيانات صحيحة
- [ ] تم اختبار الاتصال بنجاح
- [ ] تم تشغيل migrations

---

## 🎯 سكربت سريع (انسخ والصق)

```bash
# 1. افتح MySQL
mysql -u root -p

# 2. في MySQL، نفّذ:
CREATE DATABASE IF NOT EXISTS foodgo CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER IF NOT EXISTS 'laravel'@'localhost' IDENTIFIED BY 'your_secure_password';
GRANT ALL PRIVILEGES ON foodgo.* TO 'laravel'@'localhost';
FLUSH PRIVILEGES;
EXIT;

# 3. حدّث ملف .env
# DB_DATABASE=foodgo
# DB_USERNAME=laravel
# DB_PASSWORD=your_secure_password

# 4. اختبر الاتصال
php artisan db:show

# 5. شغّل migrations
php artisan migrate
```

---

## 🔍 فحص الأخطاء

إذا استمرت المشكلة:

```bash
# 1. تحقق من سجلات Laravel
tail -f storage/logs/laravel.log

# 2. تحقق من إعدادات قاعدة البيانات
php artisan config:show database

# 3. اختبر الاتصال مباشرة
php artisan tinker
>>> DB::connection()->getPdo();
```

---

## ✅ بعد حل المشكلة

1. نظف الكاش:
   ```bash
   php artisan config:clear
   php artisan cache:clear
   ```

2. اختبر الموقع مرة أخرى

---

**🎉 يجب أن يعمل الاتصال بقاعدة البيانات الآن!**

