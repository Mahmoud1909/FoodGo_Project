# ⚡ حل سريع لمشكلة قاعدة البيانات

## 🎯 المشكلة
```
Access denied for user 'laravel'@'localhost' to database 'foodgo'
```

---

## 🚀 الحل السريع (3 خطوات)

### **الخطوة 1: افتح MySQL**

```bash
mysql -u root -p
```

### **الخطوة 2: نفّذ الأوامر التالية**

انسخ والصق في MySQL:

```sql
CREATE DATABASE IF NOT EXISTS foodgo CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER IF NOT EXISTS 'laravel'@'localhost' IDENTIFIED BY 'your_password';
GRANT ALL PRIVILEGES ON foodgo.* TO 'laravel'@'localhost';
FLUSH PRIVILEGES;
EXIT;
```

**⚠️ غيّر `your_password` بكلمة مرور قوية!**

### **الخطوة 3: حدّث ملف `.env`**

افتح ملف `.env` وعدّل:

```env
DB_DATABASE=foodgo
DB_USERNAME=laravel
DB_PASSWORD=your_password    # نفس كلمة المرور من الخطوة 2
```

---

## ✅ اختبر الاتصال

```bash
php artisan db:show
```

أو:

```bash
php artisan tinker
>>> DB::connection()->getPdo();
>>> exit
```

---

## 🔄 إذا استمرت المشكلة

### **الحل البديل: استخدام root (للتطوير فقط!)**

⚠️ **تحذير:** للتطوير المحلي فقط!

```env
DB_USERNAME=root
DB_PASSWORD=your_root_password
```

---

## 📋 أو استخدم ملف SQL الجاهز

```bash
mysql -u root -p < setup_database.sql
```

ثم عدّل كلمة المرور في الملف قبل التنفيذ!

---

**🎉 يجب أن يعمل الآن!**

