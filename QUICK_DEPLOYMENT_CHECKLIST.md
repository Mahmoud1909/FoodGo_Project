# ✅ قائمة التحقق السريعة للنشر

## قبل الرفع على الاستضافة

- [ ] تحديث `.env`:
  - `APP_ENV=production`
  - `APP_DEBUG=false`
  - `APP_URL=https://yourdomain.com`
  - بيانات قاعدة البيانات الصحيحة
  - إعدادات البريد الإلكتروني

- [ ] إنشاء `APP_KEY`:
  ```bash
  php artisan key:generate
  ```

- [ ] بناء الأصول:
  ```bash
  npm install
  npm run build
  ```

- [ ] تحديث التبعيات:
  ```bash
  composer install --optimize-autoloader --no-dev
  ```

## على الخادم (Server)

- [ ] رفع الملفات (Git أو FTP)
- [ ] تثبيت التبعيات:
  ```bash
  composer install --optimize-autoloader --no-dev
  npm install && npm run build
  ```
- [ ] إعداد قاعدة البيانات:
  ```bash
  php artisan migrate --force
  ```
- [ ] تحسين الأداء:
  ```bash
  php artisan config:cache
  php artisan route:cache
  php artisan view:cache
  ```
- [ ] إعداد الصلاحيات:
  ```bash
  chmod -R 755 storage bootstrap/cache
  ```
- [ ] تفعيل SSL (HTTPS)

## بعد النشر

- [ ] اختبار تسجيل الدخول
- [ ] اختبار جميع الصفحات الرئيسية
- [ ] اختبار رفع الملفات
- [ ] اختبار إرسال البريد الإلكتروني
- [ ] إعداد النسخ الاحتياطي التلقائي

---

## 🚀 استخدام سكريبت النشر

### على Linux/Mac:
```bash
chmod +x deploy.sh
./deploy.sh
```

### على Windows:
```cmd
deploy.bat
```

---

## 📞 في حالة المشاكل

1. تحقق من `storage/logs/laravel.log`
2. تحقق من صلاحيات الملفات
3. تحقق من إعدادات قاعدة البيانات
4. تأكد من تفعيل `mod_rewrite` (Apache) أو إعداد Nginx بشكل صحيح

